# Validation Report

The `ValidationReport` is the core output object returned by every Sumeh execution engine. It contains aggregate metrics, detailed rule-level results, and provides methods for single-pass data bifurcation (splitting clean and quarantined data).

::: sumeh.core.models.validation